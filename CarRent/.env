# Chế độ online - dùng MongoDB Atlas (cần mạng)
# MONGODB_URI=mongodb+srv://dbBhung:bahung123@cluster0.pvvunny.mongodb.net/CartRent?appName=Cluster0

# Chế độ offline - dùng MongoDB local (không cần mạng)
MONGODB_URI=mongodb://127.0.0.1:27017/CartRent

# JWT
JWT_SECRET=carrent_super_secret_key_2024
JWT_EXPIRES_IN=7d

# CORS - dùng * để cho phép tất cả, hoặc chỉ định domain cụ thể VD: http://localhost:8081
CORS_ORIGIN=*

# Server
PORT=3000
